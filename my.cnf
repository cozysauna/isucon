[mysqld]

# slow_queryを出力する
slow_query_log = 1

# logを出力する場所
slow_query_log_file = /var/log/mysql/mysqlslow.log

# 全てのクエリを集計
long_query_time = 0

# 1秒ごとにフラッシュ
innodb_flush_log_at_trx_commit = 2

# バイナリログを無効化する
disable-log-bin = 1
